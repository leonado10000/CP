#2396. Strictly Palindromic Number
def isStrictlyPalindromic(self, n: int) -> bool:
    #no such number exists
    return False
"""    #step1 check number for each base from 2 to n-2
    print("yes")

def onthisbase(self, n:int ,curr:int) -> bool:
    #step1 change in base number
    curr = (self, n,curr)
    #step2 check if palindrome
    curr = str(curr)

def DecimalToBinary(num):
        if num >= 1:
            DecimalToBinary(num // 2)
        print (num % 2, end = '')

print(DecimalToBinary(50))"""